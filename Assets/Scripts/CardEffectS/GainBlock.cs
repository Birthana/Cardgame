using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GainBlock : BaseCardEffect
{
    public override void Resolve()
    {
        // target.GetComponent<Health>().AddBlock(value);
    }
}
